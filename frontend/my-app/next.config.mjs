/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If you self-host or need custom cache handlers etc., add them here per Next docs
};

export default nextConfig;
